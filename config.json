{
  "name": "Sick Day Helper",
  "version": "1.0.0",
  "slug": "sick_day",
  "description": "Custom add-on for sick day automations and notifications.",
  "arch": ["armhf", "armv7", "aarch64", "amd64", "i386"],
  "startup": "application",
  "boot": "auto",
  "init": false,
  "map": ["config:rw"],
  "schema": {
    "log_level": "select(info|debug|warning|error|critical)"
  },
  "environment": {
    "LOG_LEVEL": "${log_level}"
  }
}
