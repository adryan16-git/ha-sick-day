{
  "name": "Sick Day Helper",
  "version": "1.0.0",
  "slug": "sick_day",
  "description": "Sick day automations",
  "arch": ["armhf", "armv7", "aarch64", "amd64", "i386"],
  "startup": "application",
  "boot": "auto",
  "schema": {
    "log_level": "select(info|debug|warning|error)"
  },
  "environment": {
    "LOG_LEVEL": "${log_level}"
  }
}
